<Page
    x:Class="E.ExploreDeezer.UWP.Views.TracklistView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:E.ExploreDeezer.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:E.ExploreDeezer.UWP.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        
        <TextBlock Grid.Row="0"
                   Text="{x:Bind ViewModel.PageTitle}"
                   Margin="10,0,0,0"
                   FontSize="32"/>

        <Pivot Grid.Row="1">

            <!-- Tracklist -->
            <PivotItem Header="Tracklist">

                <ListView x:Name="Tracklist"
                          ItemsSource="{x:Bind ViewModel.Tracks, Mode=OneWay}">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <controls:TrackCell />
                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>

            </PivotItem>


            <!-- Information (Title could be Album Info / Playlist Info -->

            <PivotItem Header="Information">

                <controls:TracklistInformation
                            DataContext="{x:Bind ViewModel.InformationViewModel, Mode=OneWay}" />

            </PivotItem>



            <!-- Related content -->

            <!--
                <PivotItem Header="Related">
                        
                </PivotItem>
                 -->

        </Pivot>

    </Grid>
    
</Page>
